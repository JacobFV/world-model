__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.DS_Store
.env
.env.*
!.env.example

# Retain dataset definitions, raw payloads/receipts, samples, final artifacts,
# and latest pointers in Git so their provenance is available in a clone.
# Only transient work and rebuildable indexes stay local.
data/*/processing/
data/*/runs/
data/*/.lock
data/.sampling.lock
*.sqlite*

# Standalone browser exports (their immutable reports live under data/*/final/).
docs/generated/
