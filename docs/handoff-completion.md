# Handoff completion ledger

Active scope: all software tasks in `RESUME-PLAN.md`, updated to the dataset-local
stage layout committed in 0e86873. Laptop sampling limits remain 100 rows/1 MiB,
with a shared 64 MiB temporary allocation. Full-scale GB10 work remains deferred.
Public push is authorized after verification. External access and empirical
validation must be reported from actual evidence, not declared complete by code.

## Workstreams

- [x] R1 durable journal review, numerical/live adapter, atomic outcomes, export,
  retained authority, usage and optional authenticated envelopes (runtime worker).
- [x] E1 labor, interest/policy, inventory/prices/demand, funded recovery and work
  limits (economy worker).
- [x] S1 signed/vector evolution, atomic timeline, explicit planar geometry and
  refinement (spatial worker).
- [x] L1 structured spaces, spawned rollouts, held-out scenarios, multiobjective
  comparison (second wave).
- [x] D1 explicit workbook extraction, DIA holdings/NAV/premium data, BEA/freight
  and access-dependent source checks (root).
- [x] D2 temporal identity, candidate/reviewed contract datasets, multimodal routing,
  public institutions/conflict/research/law/information adapters (second wave).
- [x] V1 train/validation/test benchmarks, baselines and parameter/scenario uncertainty.
- [x] U1 spatial/timeline surfaces, topology playback, standalone browser inspection.
- [x] P1 explicit coupling, temporal inputs, fidelity/work, lifecycle eligibility,
  aggregation and a cross-domain replayable example.
- [x] Release: source freeze, full and clean suites, wheel/Gym/CLI/browser checks,
  bounded acquisitions, evidence reports, docs, commit/push and remote verification.

## Decisions and review record

- The latest dataset-local layout supersedes the old raw/processing/final layout
  in the historical handoff. New adapters belong in each dataset's pipeline.py.
- Work uses branch `complete-handoff`; existing paused files are preserved and
  integrated under their original contracts. No acquired payloads go into Git.

### First-wave verification

Root independently ran 84 economy/spatial tests successfully. R1 worker reports
49 covering tests passed, including independent connections, pending takeover,
partial live-effect recovery and zero-call reset. Source freeze precedes final
publication tests because provenance rejects changing imported source.

D1/V1 progress: explicit workbook parser, three DIA local pipelines, Federal
Register/Crossref/NASA/UCDP adapters, USDA suppression-aware adapter and held-out
series benchmarks implemented. Fifteen source/benchmark parser tests passed before
adding the USDA and graph-stage regressions. Acquisitions await the final freeze.

### Acquisition and review

A frozen-source snapshot passed 484 tests (7 explicit optional/acquired skips). A
second integration snapshot caught the in-progress composition clock regression and
its port/ontology registration mismatch; these are being resolved before final checks.

Eleven source profiles retained bounded samples; six reported explicit access gaps.
Actual rows all passed adapter and closed typed-graph validation. The DIA cash row
is now cash rather than security/shares. RSS declaration checks use parser events,
so quoted HTML declarations do not masquerade as XML DTDs.

Independent review found and fixed validation-label availability leakage and a live
writer takeover race. Regression coverage verifies atomic action-attempt fencing and
takeover rejection when a provider effect appears during restore. Final full-suite
verification follows the process-composition freeze.

## Final verification

Version 0.3.0: full and clean-source suites each pass **501 tests**, with seven
explicit skips. The clean run has no resource warnings. Optional Gym checks pass
17 tests with no skips; a fresh installed-wheel environment independently passes
the Gym checker, CLI examples and spawned/sequential rollout equivalence. The wheel
built from the sdist includes all 52 dataset pipelines and excludes acquired artifacts.
Chrome DOM checks and screenshot inspection pass.

All eleven acquired samples normalize into a closed typed reference graph with
**10,955 records / 2,811 entities**. Four domain queries preserve observed source
claims and provenance. The NAV benchmark's selected drift model loses to persistence
on the untouched final 20 observations (MAE 3.181289 versus 3.093644 USD/share).

The final process review fixes knowledge-cutoff propagation, pre-storage overflow
validation and bounded lifecycle eligibility work. Generic environment observations,
checkpoint replay and both composition fidelities pass; totals remain 1,000 cents / 10 kg.

See [the machine-readable verification record](handoff-verification-2026-09-15.json),
[source access outcomes](source-access-2026-09-15.json), and
[remaining empirical/operational limits](remaining-concerns.md). Source access,
licensed feeds, full coverage, causal calibration, provider semantics and GB10 runs
remain external dependencies; they are not unfinished laptop software tasks.
