# Bounded source sampling

User authorizes real sample downloads, including full files temporarily stored
within a bounded disk budget. Process sequentially, with 64 MiB temporary download
space per source, 1 MiB retained sample and 100 rows. API responses use narrower
limits where possible. Preserve acquisition criteria and original checksums;
discard full temporary downloads. Samples must not replace full dataset pointers.

- [x] Add behavioral tests for row/byte limits, oversized responses, compressed
  members, error payloads, cleanup, and isolation from full-data raw pointers.
- [x] Implement bounded disk acquisition, JSON/CSV/XLSX/ZIP sample extraction,
  profiles and per-dataset sample manifests; add CLI sample/explore commands.
- [x] Configure all catalog entries with explicit source-specific criteria,
  URLs, auth requirements or local/derived sampling policy.
- [x] Fetch sequentially; inspect returned schemas, values, missingness and
  blockers. Record observed results and limitations in a report.
- [x] Verify tests and retained footprint; document reproducible commands.

Verification: 36 tests pass; all 18 policies validated, 15 samples verified
(11 real source families plus four fixture/derived examples), three documented
blockers, and no temporary sampling downloads remain.
