# Concern resolution

User authorizes addressing all documented concerns and parallel implementation. Preserve source/evidence truth: new mechanisms do not prove calibration or complete data coverage. Each concern must receive implemented mitigation, verified capability, or a precise external dependency; no blanket completion claim for unknown evidence/licensing.

Implementation streams:
1. Transactional incremental temporal execution: persistent state, RNG, memories, held pressures, next-due times; portable checkpoints and rollback; replay remains reference oracle. Existing Environment protocol gains stateful evaluator support without repeating live agent calls.
2. Closed commercial-bank economy: dynamic production/purchasing policy inputs coupled to deposits, credit and settlement; accounting checks each step; deterministic scenario and materialization integration.
3. Disk-backed field/topology and lifecycle: bounded SQLite spatial/state reads, atomic birth/death/merge application, explicit conservation and coordinate conventions; numerical limitations disclosed.
4. Distribution: standalone wheel includes catalog and fixtures, configurable writable data root, portable provenance, clean-install smoke tests and offline fixture coverage.
5. RL/perception: explicit spaces adapter, vector execution, bounded train/evaluate routine, observation masks/delays/noise with reproducible seeds; optional external Gymnasium boundary.
6. Evidence/reconciliation/validation: acquire feasible bounded missing references, source access and license inventory, dated conflict policies with audit, sensitivity/holdout/accounting and model-validity gates. No invented contract state or claims of causal validation.
7. Interactive visual surfaces: source-preserving filtering, linked entity selection and temporal playback, explicit layers, portable standalone output.
8. Licensing: use user's selected code license; dataset terms remain separate and unknown rights remain recorded for downstream redistribution review.

Retain laptop source sample limits <=100 rows/1MiB, shared64MiB download buffer. Source code freezes before final publications. Python stdlib runtime unless explicit optional adapter; test-first workflow. Continue on main workspace with separate file ownership, no competing commits; root commits/pushes after verification under existing authorization.
