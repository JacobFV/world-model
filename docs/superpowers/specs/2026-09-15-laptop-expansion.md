# Laptop-scale continuation

User authorizes continued implementation on every front except full-scale GB10 execution. Preserve the existing typed evidence, process, materialization and explicit action/observation/reward contracts. This extends the approved architecture through independently testable components; no additional approval gate applies.

## Deliverables

1. Durable runtime operations: local SQLite execution journal, monotonic shared budgets and idempotent effect records with explicit pending/uncertain/completed states. Support bounded checkpoint persistence and history pagination. Do not claim exactly-once external effects without provider cooperation or silently retry uncertain calls.
2. Economic mechanisms: optional labor constraints, deposit-paid interest, policy-rate transmission and price/demand feedback in the coupled economy; inventory accounting and collateral recovery only with explicit consistent ledger treatment. Preserve old example behavior when options are absent.
3. Spatial dynamics: bounded signed/vector diffusion and advection with conservation and stable numerical steps. Integrate dated support lifecycle transitions into a bounded spatial evolution operation; explicitly state coordinate/frame and boundary assumptions.
4. RL: useful declared structured spaces and reproducible concurrent episode execution, maintaining finite outputs, failure boundaries and work budgets. Distinct scenario evaluation in addition to disjoint random seeds.
5. Evidence: investigate accessible primary-source replacements for blocked datasets, acquire only configured samples, normalize dated finance/reference evidence without inventing bilateral contracts. Keep source terms and missing access conditions visible.
6. Validation: train/validation/test separation for descriptive model selection, rolling predictions and empirical baselines, evaluation across scenario changes; no tuning on final holdout or automatic causal claims.
7. Surfaces: surface spatial topology/field state with declared geometry and meaningful temporal controls, retaining source evidence and bounded rendering.

## Limits and integration

Core Python stdlib; Gymnasium optional. Source samples <=100 rows and1MiB retained,64MiB shared temporary disk budget. Local SQLite and modest synthetic workloads only; no distributed or full-scale GB10 execution. Preserve source code snapshots and immutable inputs for published results. Freeze package sources before final artifact generation. Root owns integration and Git publication, agents own disjoint file sets. Missing feeds and empirical limitations are reported precisely, not hidden behind successful synthetic tests.
